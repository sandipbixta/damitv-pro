// Viewer utilities - disabled (frontend only)

export const getMatchViewerCount = async (matchId: string): Promise<number> => {
  return 0;
};

export const getBatchViewerCounts = async (matchIds: string[]): Promise<Record<string, number>> => {
  return {};
};
