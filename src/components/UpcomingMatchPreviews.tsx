// Upcoming match previews - disabled (frontend only, no Supabase)
import React from 'react';

const UpcomingMatchPreviews: React.FC = () => {
  // No match previews without database - return null
  return null;
};

export default UpcomingMatchPreviews;
