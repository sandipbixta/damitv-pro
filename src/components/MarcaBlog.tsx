// Marca Blog - disabled (frontend only, no Supabase edge functions)
import React from 'react';

const MarcaBlog: React.FC = () => {
  // Marca news fetching disabled - return null
  return null;
};

export default MarcaBlog;
